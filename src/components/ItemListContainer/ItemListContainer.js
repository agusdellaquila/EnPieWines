import '../ItemListContainer/ItemListContainer.css'
const ItemListContainer = (props) => {
    return (
        <h2 className="greeting centered">{props.greeting}</h2>
    )
}

export default ItemListContainer